# if the column cannot have a NULL value
NOT NULL 

# if the values in a column are different
UNIQUE 

PRIMARY KEY

# to prevent actions that would destroy links between tables
FOREIGN KEY

# the values in a column satisfies a specific condition
CHECK

# to set a default value for a column if no value is specified
DEFAULT

# to create and retrieve data from the database very quickly
CREATE INDEX
