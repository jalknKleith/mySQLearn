--to see the smallest value
SELECT MIN(colName) FROM tableName WHERE condition;
SELECT MIN(colName) AS aliasName FROM tableName WHERE condition;

--to see the largest value
SELECT MAX(colName) FROM tableName WHERE condition;
SELECT MAX(colName) AS aliasName FROM tableName WHERE condition;
